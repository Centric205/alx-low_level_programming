#include <stdio.h>
/**
 * main - Program Entry point
 *
 * Return: Always 0 upon successful execution
 */
int main(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}

putchar('\n');

return (0);

}
